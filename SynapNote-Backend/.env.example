# Copy this file to .env and fill in the real values (DO NOT commit .env to git)

# Server
PORT=5000
NODE_ENV=development

# MongoDB (example for Atlas)
# Replace <username>, <password>, <cluster-url> and optionally the default DB name
# e.g. mongodb+srv://myuser:mypassword@cluster0.abcde.mongodb.net/intellinotes?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://bhavesh:bhavesh@cluster0.omcvj5s.mongodb.net/?appName=Cluster0

# JWT
JWT_SECRET=222bbbd222
JWT_EXPIRE=7d

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# Email (optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@example.com
EMAIL_PASS=your-email-password
